// This placeholder replaces a previous non-runnable backup file that broke the
// Next.js build. It intentionally renders nothing but keeps the filename so
// the repository doesn't lose history or references.

import React from 'react';

export default function IndexCorruptPlaceholder() {
	return null;
}
